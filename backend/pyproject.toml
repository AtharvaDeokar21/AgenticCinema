[project]
name = "agentic-cinema-backend"
version = "0.1.0"
description = "Agentic Cinema - AI-powered agentic production platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "integration: tests requiring external services or APIs",
]

[tool.ruff]
line-length = 100